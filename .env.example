# Strapi API Configuration
# Add this to your .env.local file to connect to your Strapi server
# Example: VITE_STRAPI_URL=http://localhost:1337
VITE_STRAPI_URL=

# Notes:
# - When VITE_STRAPI_URL is not set, the app uses mock data
# - Once set, the app will fetch data from your Strapi server
# - Make sure your Strapi server is running and accessible at the URL
# - The API endpoints should be under /api route (e.g., /api/plans, /api/patients, /api/exercises, /api/doctor)
